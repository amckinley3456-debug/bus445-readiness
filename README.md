# bus445-readiness

## Git Commands

- **git add**: Makes a file, marking it as ready to be included in the next commit. It doesn't save anything permanently yet.
- **git commit**: Takes everything that's staged and saves it as a snapshot in the project's history, along with a message describing what changed.
- **git push**: Uploads the local commits to GitHub so they're stored online and visible in the repository.